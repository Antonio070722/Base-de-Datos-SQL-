-- Crear BDD
CREATE DATABASE PRUEBA;

-- Creación de tabla
CREATE TABLE ALUMNO(
    MATRICULA INT PRIMARY KEY,
    NOMBRE VARCHAR(30),
    F_NAC DATE,
    DIRECCION VARCHAR(50),
    LOCALIDAD VARCHAR(30)
);

CREATE TABLE EMPLEADO(
    NOMBRE VARCHAR(30) PRIMARY KEY,
    EDAD TINYINT,CHECK (EDAD BETWEEN 18 AND 35),
    COD_PROVINCIA TINYINT,
    FOREIGN KEY (COD_PROV) REFERENCES PROVINCIA(CODIGO) ON DELETE CASCADE,
);

CREATE TABLE PROVINCIA(
    CODIGO TINYINT PRIMARY KEY,
    NOMBRE VARCHAR(30)
);


-- Modificar datos de una tabla
ALTER TABLE TRABAJADOR ADD (DIRECCION VARCHAR(30));


ALTER TABLE TRABAJADOR ADD (APELLIDO1 VARCHAR(20), APELLIDO2 VARCHAR(20));


-- Cambiar nombre de la tabla
